ACLOCAL_AMFLAGS = -I m4
    SUBDIRS = src
    EXTRA_DIST = autogen.sh

DISTCLEANFILES = aclocal.m4 ar-lib compile config.guess config.sub config.status \
	depcomp libtool ltmain.sh missing Makefile.in

distclean-local:
	rm -rf autom4te.cache
